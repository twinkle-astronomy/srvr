services:
  srvr:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    init: true
    volumes:
      - bash-history:/home/dev/.bash_history_dir
      - .:/app
      - ${HOME}/.pi/agent:/home/dev/.pi/agent
    ports:
      - "8080:8080"
      - "443:443"
    environment:
      - PROMETHEUS_URL=http://prometheus:9090
      - DATABASE_URL=sqlite:///app/.data/data.db
      - TZ=America/Los_Angeles
      - OLLAMA_HOST=xavier.lan:11434
      # Browser E2E tests (tests/browser_e2e.rs) drive the `chrome` service.
      - WEBDRIVER_URL=http://chrome:4444
      - IMAGE_SIGNATURE_SECRET=dev
    command:
      "sleep infinity"

  # Headless Chromium exposed as a WebDriver endpoint for the browser E2E tests.
  # Its own image (Dockerfile `chrome` stage) so no browser ships in the dev
  # image; reachable on the compose network as `chrome:4444`.
  chrome:
    build:
      context: .
      dockerfile: Dockerfile
      target: chrome
    init: true
  srvr-published:
    profiles: ["published"]
    sysctls:
      net.ipv4.tcp_keepalive_time: 60
      net.ipv4.tcp_keepalive_intvl: 10
      net.ipv4.tcp_keepalive_probes: 6
    image: ghcr.io/twinkle-astronomy/srvr:0.1.8
    build:
      context: .
      dockerfile: Dockerfile
      target: publish
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    volumes:
      - srvr-data:/data
      # - ./certs:/certs:ro  # Mount certificate directory for manual TLS
    init: true
    ports:
      - "80:8080"
      - "443:443"
    environment:
      - PROMETHEUS_URL=http://prometheus:9090
      - DATABASE_URL=sqlite:///data/data.db
      - TZ=America/Los_Angeles
      - IMAGE_SIGNATURE_SECRET=something_secret
      ## Manual TLS — provide cert and key files:
      # - TLS_CERT_PATH=/certs/fullchain.pem
      # - TLS_KEY_PATH=/certs/privkey.pem
      ## ACME / Let's Encrypt — automatic certificate management:
      # - ACME_DOMAIN=example.com
      # - ACME_EMAIL=admin@example.com
      # - ACME_CACHE_DIR=/data/acme
      # - ACME_STAGING=true
      ## Shared TLS settings:
      # - HTTPS_PORT=443

  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    ports:
      - "9090:9090"
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--enable-feature=promql-experimental-functions"

volumes:
  bash-history:
  prometheus-data:
  srvr-data:
