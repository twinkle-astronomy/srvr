# Prometheus Time-Range Query Support

Users can query Prometheus over a time range (e.g. the past hour) rather than only at an instant, and use that data to render trends and graphs on their e-ink display.
