# AI-Assisted Template Generation

Use a local LLM (Ollama is already configured via `OLLAMA_API_BASE` in `.env`) to help generate or suggest Liquid SVG templates based on a natural-language description. Could also power device naming suggestions.
