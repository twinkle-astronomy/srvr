# AI-Friendly Documentation

Created `CLAUDE.md`, `CONVENTIONS.md`, and a `docs/` directory with topic-split reference files (database, server-functions, models, frontend, migrations, testing, development-process, projects) to give AI coding assistants accurate, loadable context. Updated `.aider.conf.yml` to auto-read `CONVENTIONS.md`.
